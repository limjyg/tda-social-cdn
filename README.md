# TDA social media CDN

Public image host for the Two Doctors organic social pipeline (`~/TDA/social-pipeline`).

Instagram image containers must fetch media from a public HTTPS URL, so the publisher copies
each post's media into `posts/<post-folder>/`, pushes, and hands Meta the raw.githubusercontent
URL. Everything here is about to be public on Instagram anyway - never put anything else in it.
